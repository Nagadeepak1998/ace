**Upload guidelines**

• **Maximum file size:** 25 MB  
• **Allowed filename characters:**  
  – Letters (A – Z / a – z)  
  – Numbers (0 – 9)  
  – Underscore ( _ ), hyphen (-), or period (.)  
• Any other character ( ! @ # $ % ^ & * ( ) + = { } [ ] | \ : ; " ' < > , ? / (space) ) will block the upload
